#include QMK_KEYBOARD_H

const uint16_t PROGMEM keymaps[][MATRIX_ROWS][MATRIX_COLS] = {
      [0] = LAYOUT(
          KC_Z,
          KC_X,
          KC_NO
      ),
      [1] = LAYOUT(
          _______,
          _______,
          KC_NO
      ),
      [2] = LAYOUT(
          _______,
          _______,
          KC_NO
      ),
      [3] = LAYOUT(
          _______,
          _______,
          KC_NO
      )
};
